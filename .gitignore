node_modules/
dist/
.superpowers/
*.log
.DS_Store

# Playwright artifacts
playwright-report/
test-results/

# Git worktrees (isolated feature workspaces)
.worktrees/

# Local env files (never commit secrets; use .env.example as the template)
.env
.env.local
.env.*.local
deploy/.env
deploy/.env.local

# CMS local development database + uploaded media
cms/cms.db
cms/cms.db-journal
cms/media/

# TypeScript incremental build cache
*.tsbuildinfo
*.tsbuildinfo.*

# Debug / tmp artifacts
.tmp-debug/
